# Noctalia theming — the block below is managed by `kitten themes` (Noctalia's hook).
# current-theme.conf is a symlink to themes/noctalia.conf, regenerated on scheme change.

# BEGIN_KITTY_THEME
# Noctalia
include current-theme.conf
# END_KITTY_THEME